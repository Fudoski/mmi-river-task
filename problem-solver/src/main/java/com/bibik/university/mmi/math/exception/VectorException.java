package com.bibik.university.mmi.math.exception;

public class VectorException extends IllegalArgumentException {
    public VectorException(String errorMessage) {
        super(errorMessage);
    }
}
